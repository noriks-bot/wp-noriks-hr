<?php
/**
 * product-bottom: NORIKSHERS (orto-norikshers)
 *
 * Dedicated bottom-nicer for the NORIKS "norikshers" product.
 * Shown via single-product-bottom-nicer.php when noriks_is_type('norikshers').
 *
 * SCAFFOLD — sekcije i videi se dodaju kad korisnik dostavi materijale
 * (mediji u temu: img/norikshers*). Zasad ne ispisuje ništa vidljivo.
 */
if ( ! defined( 'ABSPATH' ) ) { exit; }

$nh_v = get_template_directory_uri() . '/img/norikshers-videos/';
// TODO: sekcije (why + videi) — u pripremi.
?>
